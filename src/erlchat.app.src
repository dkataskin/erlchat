{application, erlchat,
          [{description, "Private message system for web apps in Erlang."},
            {vsn, "0.1"},
            {modules, []},
            {registered, []},
            {applications, [kernel, stdlib, cowboy]},
            {mod, {erlchat_app, []}},
            {env, []}]}.